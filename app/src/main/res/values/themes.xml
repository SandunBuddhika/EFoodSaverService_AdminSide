<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EFoodSaver" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green_green</item>
        <item name="fontFamily">@font/nacelle_regular</item>
        <item name="colorSecondary">@color/ash</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Theme.EFoodSaverService" parent="Base.Theme.EFoodSaver" />
</resources>
